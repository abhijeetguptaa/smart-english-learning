export const TTT_TIMER_DURATION = 30; // seconds per turn in PvAI mode
export const TTT_AI_THINK_DELAY_MS = 400; // ms delay to simulate AI thinking
export const TTT_RESTART_DELAY_MS = 2000; // ms delay before auto-restart after game end

export default {
  TTT_TIMER_DURATION,
  TTT_AI_THINK_DELAY_MS,
  TTT_RESTART_DELAY_MS,
};
